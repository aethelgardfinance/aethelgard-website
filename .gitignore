node_modules/
dist/
.env
.env.*
!.env.example
.DS_Store


.vercel

# Astro build cache — per-machine
.astro/

# Claude Code local data (permissions, memory, sessions) — per-machine, not for sharing
.claude/
